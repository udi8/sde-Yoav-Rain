# מד-גשם שדה יואב — הנחיות לעבודה על הפרויקט

## Mobile-first — דרישת קבע

**כל UI שנבנה בפרויקט הזה — קיים או עתידי — חייב לעבוד ולהיראות טוב במסך טלפון נייד**, לא רק בדסקטופ. זו לא בקשה חד-פעמית אלא דרישה קבועה לכל שינוי:

- לפני שמסמנים feature/עמוד/קומפוננטה כגמורים — לבדוק אותם ברוחב מסך צר (~360–390px), לא רק בדסקטופ.
- להשתמש ביחידות/פריסה גמישות (flexbox/grid, `%`/`fr`, לא רוחב קבוע ב-`px` לקונטיינרים), עם `@media` breakpoints כשצריך (ראה `PublicPage.css`, `AdminPage.css` לדוגמאות קיימות).
- טבלאות רחבות עוטפים ב-container עם `overflow-x: auto` כדי שלא ייווצר גלילה אופקית של כל העמוד (ראה `.readings-list`, `.admins-table-wrap`).
- כפתורים/שדות קלט צריכים tap target סביר (לא זעיר מדי) ו-`font-size` שלא גורם ל-zoom אוטומטי באייפון (16px ומעלה לשדות טקסט).
- ה-`viewport` meta כבר מוגדר ב-`index.html` — לא לגעת בו בלי סיבה טובה.

## מבנה הרשאות מנהלים

הרשאת מנהל היא data-driven דרך קולקציית Firestore `admins` (לא רשימה קשיחה בקוד) — ראה README.md סעיף "הרשאות מנהלים". מנהל קיים מוסיף/מסיר מנהלים דרך `/admin` → טאב "ניהול מנהלים". המנהל הראשון נזרע ידנית עם `scripts/bootstrapAdmin.js`.

## סטאק

React + Vite, Firebase (Auth + Firestore + Hosting), טיר Spark (חינמי) — בלי Cloud Functions. RTL בעברית לכל האתר.
